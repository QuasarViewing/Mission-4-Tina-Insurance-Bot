version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - GEMINI_API_KEY=your_gemini_api_key_here

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend